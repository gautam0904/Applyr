# Frontend Environment Variables

# For local development (matching environment.ts)
API_URL=http://localhost:3000/api

# For production (Render)
# The frontend is served as static files, so it will use relative paths or 
# you can use a build-time replacement if you have a specific backend URL.
# By default, I set environment.prod.ts to use '/api' (proxy/same-domain).
